{
  "name": "ev-trip-planner",
  "version": "0.1.0",
  "private": true,
  "description": "Pianificatore di viaggi per auto elettriche: percorso migliore ed economico, con colonnine, POI e pedaggi (Europa, API aperte).",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "setup": "npm install && npm --workspace backend run db:setup",
    "test": "npm --workspace backend run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
